apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubevista
  labels: { app.kubernetes.io/name: kubevista }
automountServiceAccountToken: true
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kubevista-reader
rules:
  {{- if .Values.topologyRBACDiscovery }}
  - apiGroups: ["rbac.authorization.k8s.io"]
    resources: ["roles", "clusterroles", "rolebindings", "clusterrolebindings"]
    verbs: ["list"]
  {{- end }}
  - nonResourceURLs: ["/readyz"]
    verbs: ["get"]
  - apiGroups: [""]
    resources: ["nodes", "namespaces", "pods", "pods/log", "events", "services"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "daemonsets", "statefulsets", "replicasets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["discovery.k8s.io"]
    resources: ["endpointslices"]
    verbs: ["get", "list"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses", "networkpolicies"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubevista-reader
roleRef: { apiGroup: rbac.authorization.k8s.io, kind: ClusterRole, name: kubevista-reader }
subjects: [{ kind: ServiceAccount, name: kubevista, namespace: "{{ .Release.Namespace }}" }]
---
{{- if .Values.operations.enabled }}
{{- range .Values.operations.namespaces }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kubevista-operator
  namespace: {{ . | quote }}
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch"]
  - apiGroups: ["apps"]
    resources: ["deployments/scale"]
    verbs: ["get", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kubevista-operator
  namespace: {{ . | quote }}
roleRef: { apiGroup: rbac.authorization.k8s.io, kind: Role, name: kubevista-operator }
subjects: [{ kind: ServiceAccount, name: kubevista, namespace: "{{ $.Release.Namespace }}" }]
---
{{- end }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubevista-api
  labels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api }
spec:
  replicas: {{ .Values.replicas }}
  strategy: { type: RollingUpdate, rollingUpdate: { maxUnavailable: 0, maxSurge: 1 } }
  selector: { matchLabels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api }
      annotations: { prometheus.io/scrape: "true", prometheus.io/port: "8080" }
    spec:
      serviceAccountName: kubevista
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile: { type: RuntimeDefault }
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector: { matchLabels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api } }
      containers:
        - name: api
          {{- if .Values.image.digest }}
          image: "{{ .Values.image.repository }}@{{ .Values.image.digest }}"
          {{- else }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          {{- end }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports: [{ name: http, containerPort: 8080 }]
          env:
            - { name: KUBEVISTA_ENVIRONMENT, value: production }
            - { name: KUBEVISTA_VERSION, value: "{{ .Chart.AppVersion }}" }
            - { name: KUBEVISTA_CLUSTER_NAME, value: {{ .Values.clusterName | quote }} }
            - { name: KUBEVISTA_DEMO_MODE, value: "false" }
            - { name: KUBEVISTA_ARGO_DELIVERY, value: {{ .Values.deliveryArgo | quote }} }
            - { name: KUBEVISTA_GITHUB_DELIVERY, value: {{ .Values.deliveryGitHub | quote }} }
            - { name: KUBEVISTA_OPERATIONS_ENABLED, value: {{ .Values.operations.enabled | quote }} }
            - { name: KUBEVISTA_OPERATION_NAMESPACES, value: {{ join "," .Values.operations.namespaces | quote }} }
            - { name: KUBEVISTA_MIN_REPLICAS, value: {{ .Values.operations.minReplicas | quote }} }
            - { name: KUBEVISTA_MAX_REPLICAS, value: {{ .Values.operations.maxReplicas | quote }} }
            {{- if .Values.operations.enabled }}
            - { name: KUBEVISTA_ALB_SIGNER_ARN, value: {{ required "operations.albSignerArn is required when guarded operations are enabled" .Values.operations.albSignerArn | quote }} }
            {{- end }}
            - { name: KUBEVISTA_TRACE_SAMPLE_RATIO, value: {{ .Values.telemetry.traceSampleRatio | quote }} }
            - { name: OTEL_EXPORTER_OTLP_ENDPOINT, value: {{ .Values.telemetry.otlpEndpoint | quote }} }
          readinessProbe: { httpGet: { path: /readyz, port: http }, periodSeconds: 5 }
          livenessProbe: { httpGet: { path: /healthz, port: http }, initialDelaySeconds: 10 }
          resources: {{ toYaml .Values.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
            readOnlyRootFilesystem: true
---
apiVersion: v1
kind: Service
metadata: { name: kubevista-api, labels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api } }
spec:
  selector: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api }
  ports: [{ name: http, port: 80, targetPort: http }]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: kubevista-api }
spec:
  minAvailable: 1
  selector: { matchLabels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api } }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: kubevista-api }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: kubevista-api }
  minReplicas: {{ .Values.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: {{ .Values.autoscaling.cpuTarget }} }
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: kubevista-api }
spec:
  podSelector: { matchLabels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api } }
  policyTypes: [Ingress, Egress]
  ingress:
    - from: [{ podSelector: { matchLabels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: web } } }]
      ports: [{ protocol: TCP, port: 8080 }]
    - from: [{ namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: observability } } }]
      ports: [{ protocol: TCP, port: 8080 }]
    - from: [{ podSelector: { matchLabels: { app.kubernetes.io/component: smoke-test } } }]
      ports: [{ protocol: TCP, port: 8080 }]
  egress:
    - to: [{ namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: kube-system } } }]
      ports: [{ protocol: UDP, port: 53 }, { protocol: TCP, port: 53 }]
    - to: [{ ipBlock: { cidr: {{ .Values.networkPolicy.apiServerCidr | quote }} } }]
      ports: [{ protocol: TCP, port: 443 }]
    - to: [{ namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: observability } } }]
      ports: [{ protocol: TCP, port: 4317 }]
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kubevista-api
  labels: { release: monitoring }
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  selector:
    matchLabels: { app.kubernetes.io/name: kubevista, app.kubernetes.io/component: api }
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
