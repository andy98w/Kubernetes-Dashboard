terraform {
  backend "s3" {
    bucket       = "REPLACE_WITH_BOOTSTRAP_OUTPUT"
    key          = "kubevista/dev/terraform.tfstate"
    region       = "us-west-2"
    encrypt      = true
    use_lockfile = true
  }
}

